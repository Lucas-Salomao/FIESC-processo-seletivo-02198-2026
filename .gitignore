# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Segredos e credenciais
.env
*.json.key
credentials/
service-account*.json

# Artefatos gerados
# Os .joblib do modelo SÃO versionados (74 MB): a imagem de deploy precisa
# deles e o build roda a partir do repositório. Em um cenário com mais
# iterações de modelo, o lugar deles seria um registry de artefatos.
artifacts/*.pkl
data/chroma/
*.log

# Dados brutos (fornecidos pela prova — manter fora do versionamento)
documentos/banner.csv
documentos/banner.xlsx

# IDE / SO
.vscode/
.idea/
Thumbs.db
.DS_Store

# Enunciado da prova — documento do avaliador, não faz parte do código-fonte
documentos/Prova*

# Node: usado apenas pelo SDK de infraestrutura como código do Railway
node_modules/

# Scaffolding de ferramentas de IA (gerado pelo CLI, não é código do projeto)
.agents/
